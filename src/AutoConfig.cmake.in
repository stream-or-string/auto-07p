@PACKAGE_INIT@

# If your libraries depend on other packages (e.g. MPI), 
# you would use include(CMakeFindDependencyMacro) and 
# find_dependency(MPI) here.

# Load the targets (generated by install(EXPORT))
include("${CMAKE_CURRENT_LIST_DIR}/AutoTargets.cmake")

check_required_components("Auto")